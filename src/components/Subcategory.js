import React from 'react'
import { useGlobalContext } from "../context"

const Subcategory = () => {
  const { drinks } = useGlobalContext();

  return (
    <section className="section-subcategory">
      <div>
        
      </div>

    </section>
  )
}

export default Subcategory
